# Add project specific ProGuard rules here.
# This app has no reflection-based libraries (no Gson/Retrofit/etc.), so the
# default Android optimize rules are sufficient. Add -keep rules here if a
# reflection-based dependency is introduced later.
